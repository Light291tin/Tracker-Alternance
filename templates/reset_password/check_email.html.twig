{% extends 'base.html.twig' %}

{% block title %}Email envoyé{% endblock %}

{% block body %}
<style>
    nav.navbar { display: none !important; }
    body {
        background: url('https://images.unsplash.com/photo-1483728642387-6c3bdd6c93e5?q=80&w=2076&auto=format&fit=crop') no-repeat center center fixed; 
        background-size: cover; font-family: 'Segoe UI', sans-serif; color: white; height: 100vh; margin: 0; overflow: hidden;
    }
    .overlay { background: rgba(0, 0, 0, 0.5); position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: 1; }
    .content-wrapper { position: relative; z-index: 2; display: flex; height: 100vh; width: 100%; align-items: center; justify-content: center; }
    
    /* Carte centrée simple pour le message de succès */
    .success-card {
        background: rgba(255, 255, 255, 0.1); 
        backdrop-filter: blur(10px); 
        padding: 50px; border-radius: 20px; 
        border: 1px solid rgba(255,255,255,0.2);
        max-width: 600px; text-align: center;
    }
    h1 { font-size: 2.5rem; margin-bottom: 20px; }
    p { font-size: 1.1rem; line-height: 1.6; opacity: 0.9; margin-bottom: 30px; }
    .btn-white { background: white; color: #e65100; padding: 10px 30px; text-decoration: none; border-radius: 50px; font-weight: bold; }
    .btn-white:hover { background: #f0f0f0; }
</style>

<div class="overlay"></div>

<div class="content-wrapper">
    <div class="success-card">
        <h1>📨 Email envoyé !</h1>
        <p>
            Un email contenant un lien pour réinitialiser votre mot de passe vient d'être envoyé à l'adresse indiquée.
            <br><br>
            <em>(Pensez à vérifier vos spams si vous ne le voyez pas arriver d'ici quelques minutes).</em>
        </p>
        
        {# J'ai corrigé cette partie en mettant "1 heure" en dur au lieu de la variable #}
        <p style="font-size: 0.9rem; opacity: 0.7;">
            Note : Le lien expirera dans 1 heure.
        </p>
        
        <a href="{{ path('app_login') }}" class="btn-white">Retour à la connexion</a>
    </div>
</div>
{% endblock %}